{
  "name": "db-quiz-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "5.5.0",
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "prisma": "^5.5.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "prisma": "npx prisma generate --schema prisma/schema-app.prisma --watch && npx prisma generate --schema prisma/schema-quiz.prisma --watch",
    "start": "node build/index.js",
    "dev": "yarn && nodemon -L src/index.ts --watch \"src/**/*.{ts,js}\"",
    "test": "jest --runInBand",
    "build": "tsc -p tsconfig-build.json",
    "type-check": "tsc --noEmit",
    "lint:fix": "eslint --fix \"src/**/*.{ts,js}\"",
    "lint": "eslint \"src/**/*.{ts,js}\"",
    "docker:up": "docker-compose up -d && yarn docker:shell",
    "docker:down": "docker-compose down",
    "docker:shell": "docker exec -it api-db-quiz sh",
    "docker:build": "docker build -t docker-express-boilerplate-api ."
  },
  "devDependencies": {
    "@types/cors": "^2.8.15",
    "@types/express": "^4.17.20",
    "@types/multer": "^1.4.9",
    "eslint": "^8.52.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "joi": "^17.11.0",
    "nodemon": "^3.0.1",
    "stream": "^0.0.2",
    "ts-node": "^10.9.1"
  }
}
